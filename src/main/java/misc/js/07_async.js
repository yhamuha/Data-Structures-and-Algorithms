
    // js - one thread language

    // Event Loop
    // asynchronously


    // timeout, interval
    // as example of asynchronous in js

    // const timeout = setTimeout(()=> {
    //     console.log('after timeout')
    // }, 2500)
    // clearTimeout(timeout)
    //
    //
    // const interval = setInterval(()=> {
    //     console.log('after interval')
    // }, 1000)
    // clearInterval(interval)

    // promises - convenient work wit asynchronously in js

    // callback hell: a lot of async code with callbacks
    // promises allow to have: .then .catch .finally
    // for avoid big level sub callbacks
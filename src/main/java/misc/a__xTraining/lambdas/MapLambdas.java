package misc.a__xTraining.lambdas;

import java.util.*;
import java.util.stream.Collectors;

/**
 *
 */
public class MapLambdas {
    public static void main(String[] args) {

        // map, reduce
        /*List<Integer> list = new ArrayList<>();
        list.add(1);
        list.add(2);
        list.add(3);
        List<Integer> collect1 = list.stream().map(x->x*2).collect(Collectors.toList());
        System.out.println(collect1);
        Optional<Integer> max = list.stream().max(Comparator.naturalOrder());
        System.out.println(max);
        Optional<Integer> max2 = list.stream().reduce(Integer::max);
        System.out.println(max2);

        int maxIntPrimitive = list.stream().collect(Collectors.summarizingInt(Integer::intValue)).getMax();
        System.out.println(maxIntPrimitive);*/

        //

    }
}
